<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Application<%= typeof pageTitle !== 'undefined' ? pageTitle : "" %></title>
</title>
  </head>
  <body>
    <h1 style="background-color: orange">My Application</h1>
    <a href="/">Home</a>|<a href="/services">Services</a>|<a href="/contact"
      >Contact</a>|
 

<% if (user?.role === "admin") { %>

 <a href="/admin">Admin</a>|
  <% } %>
     <% if (!user) {  %>

      <a href="/users/login">Login</a>
<% } else { %>

  <a href="/users/logout">Logout</a>
  <% } %>
    <%- body %>
    <h3>Footer</h3>
  </body>
</html>
